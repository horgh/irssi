# #1502 — Add SET window_default_ to the "See also" section of the window help

- Commit: 363fbf04
- Verdict: SAFE

## What it changes
Adds `SET window_default_level` and `SET window_default_hidelevel` to the
"See also" line of the window help source file. One-line documentation change.

## Files touched
- docs/help/in/window.in

## Release-safety findings
None — safe to release. Documentation-only change. Both referenced settings
(`window_default_level`, `window_default_hidelevel`) actually exist in the
source (registered in fe-windows.c and gui-windows.c). No code, build, or
behavior impact.

## Interactions with related changes
None.

## NEWS accuracy
Accurate.
