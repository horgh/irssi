# #1463 — Update the GitHub Actions workflows to a newer Ubuntu runner image

- Commit: a8a24605
- Verdict: SAFE

## What it changes
Changes the CI build matrix in `.github/workflows/check.yml` from the
retired `ubuntu-18.04` runner image to `ubuntu-20.04`, both in the `os`
matrix list and in the corresponding `include` entry (which pins
`meson_ver: ==0.53.2` and `setuptools_ver: <51`).

## Files touched
- `.github/workflows/check.yml`

## Release-safety findings
None — safe to release. This affects only the CI runner image used by
GitHub Actions. It touches no source, build configuration, or shipped
artifact, so it cannot break the released product. The pinned meson and
setuptools versions are preserved.

## Interactions with related changes
None.

## NEWS accuracy
Accurate. The change updates the Ubuntu runner image used by the GitHub
Actions workflow as described.
