# #1561 — Update GitHub Actions to Ubuntu 22.04

- Commit: 39d15558
- Verdict: SAFE

## What it changes
Updates the CI matrix in `.github/workflows/check.yml`, replacing the
`ubuntu-20.04` runner image with `ubuntu-22.04` in both the matrix `os` list
and the corresponding `include` entry (which pins `meson_ver: ==0.53.2` and
`setuptools_ver: <51`). Two lines changed.

## Files touched
- .github/workflows/check.yml

## Release-safety findings
None — safe to release. This is a CI-only change. It does not touch source,
build configuration, or anything shipped to users. Worst case is a CI failure,
which cannot break a release artifact.

## Interactions with related changes
None.

## NEWS accuracy
Accurate. The change updates the GitHub Actions runner to Ubuntu 22.04 as
described.
