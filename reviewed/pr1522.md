# #1522 — Ensure all text files end with a newline

- Commit: c906b769
- Verdict: SAFE

## What it changes
Adds a single trailing newline character to the end of 5 text files. There are
zero content changes: `git diff --ignore-all-space c906b769^1 c906b769` produces
empty output, confirming every modification is purely the addition of an EOF
newline.

## Files touched
- `.github/workflows/clangformat.yml` (CI config)
- `.github/workflows/trigger-pages.yml` (CI config)
- `docs/design.html` (documentation)
- `docs/faq.html` (documentation)
- `src/irc/core/scram.h` (C header, ends with `#endif` + newline)

## Release-safety findings
None — safe to release.

The only compiled artifact affected is `src/irc/core/scram.h`. Adding a newline
after the final `#endif` is the canonical form and only removes the
"no newline at end of file" warning some toolchains emit; it cannot change
preprocessing or compilation results. The two YAML files are valid GitHub
Actions config (a trailing newline does not alter YAML semantics), and the two
HTML files are docs not involved in the build or runtime. No code, behavior,
ABI, defaults, or security surface is touched.

## Interactions with related changes
None.

## NEWS accuracy
Accurate. The change is exactly "ensure all text files end with a newline" — it
adds trailing newlines and nothing else.
