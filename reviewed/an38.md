# an#38, 9f9d49a2 — Document the Perl version() and parse_special() functions and the $abiversion special variable. This was incorrectly listed in the 1.4.1 NEWS but was not actually present until now

- Commit: 9f9d49a2
- Verdict: SAFE

## What it changes
Documentation-only change. Adds entries to `docs/perl.txt` documenting the Perl `version()`, `parse_special()`, `Server::parse_special()`, and `Windowitem::parse_special()` functions, and adds an entry to `docs/special_vars.txt` documenting the `$abiversion` special variable (mapping to `IRSSI_ABI_VERSION`). Net diff is +8 lines, no code.

## Files touched
- docs/perl.txt
- docs/special_vars.txt

## Release-safety findings
None — safe to release. The change touches only plaintext documentation files; no source, headers, build files, or runtime behavior are affected. No injection, ABI, or portability impact.

## Interactions with related changes
None. Siblings (pr1510, pr1543, pr1604, pr1606) presumably introduce/modify the actual Perl functionality being documented; this commit only documents it and does not affect their code.

## NEWS accuracy
Accurate. The diff documents exactly the `version()` and `parse_special()` Perl functions and the `$abiversion` special variable, as described.
