# #1600 — Update package lists in the abicheck GitHub Actions workflow

- Commit: 97304bb7
- Verdict: SAFE

## What it changes
In `.github/workflows/abicheck.yml`, three `sudo apt install ...` steps are prefixed with `sudo apt update;` so the package index is refreshed before installing build dependencies / `abigail-tools`.

## Files touched
- `.github/workflows/abicheck.yml`

## Release-safety findings
None — safe to release. This affects only CI ABI-check tooling, not the shipped product, its build for users, or runtime behavior. No injection or security concern (the variables are workflow-controlled, unchanged from before).

## Interactions with related changes
None.

## NEWS accuracy
Accurate.
