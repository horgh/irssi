# #1568 — Document additional compile dependencies (utf8proc, libgcrypt, complete Perl) in INSTALL

- Commit: 3c656846
- Verdict: SAFE

## What it changes
Adds four lines to the plain-text `INSTALL` document listing additional build
dependencies: a note that a complete Perl (ExtUtils::Embed and xsubpp) is
needed, plus utf8proc (optional, char width) and libgcrypt (for OTR). No code,
build system, or configuration is touched.

## Files touched
- INSTALL

## Release-safety findings
None — safe to release. This is a documentation-only change with no effect on
compiled code, build behavior, or runtime. No injection or security surface.

## Interactions with related changes
None.

## NEWS accuracy
Accurate. The NEWS line precisely describes the INSTALL additions (utf8proc,
libgcrypt, complete Perl).
